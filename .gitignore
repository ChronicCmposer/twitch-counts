# Build artifacts (do not commit)
fibonacci
twitch-counts
*.o

# Per-phase driver binaries (tc-*-test) and their check_*.o drivers
tc-*-test
check_*.o

# Generated .inc blobs are COMMITTED sources (tc_manual.inc / tc_fish.inc /
# tc_json_schema.inc) — do NOT ignore them.

# Python bytecode from the .inc generators
__pycache__/

# Per-platform build trees (objects, drivers, third-party objects, the
# pcre2 build): build/linux, build/darwin
build/

# Third-party build products (vendored SOURCES are committed; these are
# generated by the Makefile bootstrap rules / compiles)
third_party/musl/
third_party/musl-1.2.6/
third_party/musl-1.2.6.tar.gz
third_party/pcre2-10.48/
third_party/pcre2/
third_party/pcre2-10.48.tar.gz
third_party/sqlite-amalgamation.zip
third_party/sqlite3/sqlite-amalgamation-3460100/